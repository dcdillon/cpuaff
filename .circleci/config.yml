version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Update Package List
          command: apt-get update
      - run:
          name: Install Compilers
          command: apt-get install -y g++ clang
      - run:
          name: Install Development Tools
          command: apt-get install -y make autoconf automake
      - run:
          name: Install Dependencies
          command: apt-get install -y libhwloc-dev
      - run:
          name: Build with g++
          command: ./bootstrap.sh && ./configure && make && make check
      - run:
          name: Cleanup
          command: ./cleanup.sh
      - run:
          name: Build with hwloc
          command: ./bootstrap.sh && ./configure --with-hwloc && make && make check
      - run:
          name: Cleanup
          command: ./cleanup.sh
      - run:
          name: Build with clang++
          command: ./bootstrap.sh && CXX=clang++ ./configure && make && make check
